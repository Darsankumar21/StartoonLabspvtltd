{
  "version": 2,
  "builds": [
      {
          "src": "server.js",
          "use": "@vercel/node",
        "config": {
        "runtime": "nodejs18.x" // Or choose the desired Node.js version
      }
      }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/server.js"
    }
  ]
}
